const Profile = () => {
    return (
        <section className="space-y-6">
            <div>
                <p className="text-sm uppercase tracking-[0.3em] text-text-muted">Profile</p>
                <h1 className="mt-2 text-3xl font-bold text-text-main">Profile skeleton</h1>
            </div>
            <div className="rounded-2xl border border-border bg-dark-gray p-6 text-text-muted">
                User settings and LeetCode connection details will live here.
            </div>
        </section>
    );
};

export default Profile;
